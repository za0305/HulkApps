<section class="section-content">
  <div class="container">
    {% unless page.handle == 'covid-19' %}
    <div class="row">
      <div class="col-12 px-sm-4 text-center">
        <h1 class="page-title">{{ page.title }}</h1>
      </div>
    </div>
    {% endunless %}
    <div class="row">
      <div class="col-12">
        <div class="rte page-description">{{ page.content }}</div>
      </div>
    </div>
  </div>
</section>
